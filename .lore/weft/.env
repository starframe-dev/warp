# Shared non-secret model alias mapping required by the Weft pipeline.
# Never store API keys, tokens, passwords, or other secrets in this tracked file.
# Keep provider credentials in your local agent/provider credential store.
code-check-model=gpt-6-luna
