# Selectable

Тип `Selectable` добавляет поддержку выделения текста к оборачиваемому
`Panel`. Перетаскивание мышью и Shift+стрелки создают выделение, которое
отображается с инвертированными цветами и может быть извлечено как
обычный текст.

``` go
package warp

type Selectable struct {
    Content Panel
    AnchorX, AnchorY int
    CursorX, CursorY int
    HasSelection bool
    Selecting    bool
    lastW, lastH int
    lastLines    []string
}
```

## Обзор

`Selectable` оборачивает любой `Panel` и добавляет:

- Выделение мышью (нажатие/перемещение/отпускание левой кнопки)
- Выделение с клавиатуры при помощи Shift+стрелки
- Ctrl+A для выделения всего, Esc для сброса
- Копирование выделенного текста в системный буфер обмена через OSC 52

Выделенная область отображается с использованием ANSI-стиля
reverse-video (`\x1b[7m`), чтобы пользователь видел точно то, что
выделено.

## Типы

### Selectable

Публичная структура со следующими экспортированными полями:

| Поле | Тип | Описание |
|----|----|----|
| Content | `Panel` | Оборачиваемый контент-панель, к которой добавляется выделение. |
| AnchorX, AnchorY | `int` | Одна из границ выделения в координатах терминальных ячеек. |
| CursorX, CursorY | `int` | Активная end-exclusive граница; `CursorX` может быть равен ширине viewport. |
| HasSelection | `bool` | True, если существует непустое выделение. |
| Selecting | `bool` | True, пока идёт активное перетаскивание мыши. |

Не экспортированные поля `lastW`, `lastH` и `lastLines` кэшируют
последние размеры рендеринга и строки, чтобы `SelectedText` мог
извлекать текст, используя точно ту же систему координат, которая
применялась при рендеринге.

## Публичное API

``` go
func NewSelectable(content Panel) *Selectable
```

Создаёт новый `Selectable`, оборачивающий заданную панель. Всё состояние
выделения начинается пустым.

``` go
func (s *Selectable) SelectedText() string
```

Возвращает текущий выделенный текст как обычную строку. Использует
кэшированные строки рендеринга, чтобы система координат точно совпадала
с тем, что видно на экране. Если кэшированных строк нет, контент
рендерится с последними известными размерами; неположительная ширина
заменяется на `80`, а неположительная высота — на `24`.

``` go
func (s *Selectable) ClearSelection()
```

Удаляет текущее выделение, сбрасывая и `HasSelection`, и `Selecting`.

``` go
func (s *Selectable) Copy() tea.Cmd
```

Возвращает `tea.Cmd`, копирующий выделенный текст в системный буфер
обмена через OSC 52. Предназначен для вызова при нажатии Ctrl+C
пользователем. Возвращает `nil`, если выделение отсутствует.

``` go
func (s *Selectable) SelectAll(w, h int)
```

Выделяет весь видимый контент в пределах заданных ширины и высоты.

``` go
func (s *Selectable) View(w, h int) string
```

Рендерит оборачиваемый контент с подсветкой и кэширует строки для
последующего извлечения текста. Границы диапазона end-exclusive
(`[start, end)`); горизонтальные координаты ограничиваются диапазоном
`[0, w]`, поэтому допустима граница сразу после последней видимой ячейки.
Если `Content` равен nil, `View` возвращает ровно `h` пустых строк
(`""` при `h == 0`, иначе `h-1` символов перевода строки).

``` go
func (s *Selectable) Elements(width, height int) []Element
```

Прозрачно передаёт семантические элементы вложенной панели с теми же
границами и нормализованными размерами.

``` go
func (s *Selectable) Update(msg tea.Msg) tea.Cmd
```

Обрабатывает сообщения мыши и клавиатуры для выделения. Сообщения, не
потреблённые логикой выделения, проксируются в оборачиваемый `Panel`.

## Поведение

### Выделение мышью

- **Нажатие (левая кнопка):** Запоминает исходную ячейку и начинает перетаскивание.
- **Перемещение:** При движении вперёд ячейка `x` задаёт end-exclusive границу `x+1`; обратное выделение использует границу после исходной ячейки. `HasSelection` становится true.
- **Отпускание:** Финализирует end-exclusive границу. Нажатие и отпускание без движения не создают выделение.

### Выделение с клавиатуры

- **Shift+Up / Shift+Down / Shift+Left / Shift+Right:** Расширяет или
  создаёт выделение в заданном направлении. Shift+Tab намеренно *не*
  обрабатывается и проксируется в оборачиваемую панель (например,
  терминал), чтобы TUI-приложения внутри PTY его получали.
- **Ctrl+A:** Выделяет весь видимый контент (откат к 80×24, если размеры
  неизвестны).
- **Esc:** Сбрасывает выделение.

### Рендеринг

`View` применяет reverse-video к выделенному диапазону. `sortedBounds`
нормализует анкер и курсор в end-exclusive границы. Строки вне диапазона
проходят без изменений. Подсветка учитывает ширину терминальных ячеек и
целые grapheme clusters: при пересечении части широкой графемы
подсвечивается вся графема.

### Извлечение текста

`SelectedText` проходит по кэшированным строкам и извлекает целые
графемы, пересекающие выбранный диапазон терминальных ячеек. ANSI
escape-последовательности пропускаются, поэтому текст соответствует
видимому выделению. Для каждой выбранной строки добавляется фрагмент,
даже если он пустой; это сохраняет пустые строки и завершающие переводы строк.

### Обработка ANSI / Unicode

`highlightRange` и `extractVisRange` отслеживают ANSI-aware позиции в
терминальных ячейках и целые графемы, а не байты или руны. CSI и OSC
последовательности не сдвигают визуальную позицию; выделение любой
ячейки графемы включает всю графему.

## Детали реализации

- **Буфер обмена OSC 52:** Метод `Copy` кодирует выделенный текст как
  base64 и излучает последовательность `\x1b]52;c;<data>\x07`. Это
  работает в альтернативном экране Bubbletea.
- **End-exclusive границы:** `SelectAll(w, h)` задаёт последнюю горизонтальную границу как `w`, включая последнюю ячейку. Мышь и Shift+Right используют то же правило для ASCII и Unicode.
- **Очистка выделения:** Если после отпускания или шага с клавиатуры
  курсор и анкер совпадают, выделение автоматически сбрасывается.
- **Прокси-роутинг сообщений:** Любое сообщение, не обработанное логикой
  выделения (нераспознанные клавиши, Shift+Tab, сообщения изменения
  размера и т.п.), передаётся в `Content.Update(msg)`, сохраняя
  функциональность оборачиваемой панели.

## Примеры использования

``` go
sel := NewSelectable(myPanel)
model.SetProgramView(sel)

// In the model update:
case tea.KeyMsg:
    if msg.String() == "ctrl+c" {
        return sel.Copy()
    }
    if msg.String() == "esc" {
        sel.ClearSelection()
    }

// To get the selected text (e.g. for paste or display):
text := sel.SelectedText()
```
